import React from 'react';
import { Typography, Box } from '@mui/material';

const Dashboard = () => (
  <Box p={4}>
    <Typography variant="h4">Dashboard</Typography>
    <Typography>Welcome to the POS system!</Typography>
  </Box>
);

export default Dashboard; 